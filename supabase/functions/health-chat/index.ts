import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { messages, pm25, aqi, temperature, humidity, location } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    
    if (!LOVABLE_API_KEY) {
      throw new Error("LOVABLE_API_KEY is not configured");
    }

    // Extract user context from the last message if available
    const lastMessage = messages[messages.length - 1]?.content || '';
    const hasUserContext = lastMessage.includes('ข้อมูลสุขภาพผู้ใช้');

    const systemPrompt = `คุณคือ AI Health Advisor ที่อบอุ่น เห็นอกเห็นใจ และมีความรู้สูง คุณมีหน้าที่ให้คำแนะนำด้านสุขภาพที่ชัดเจน ปลอดภัย และเป็นประโยชน์แก่ผู้ใช้ โดยพิจารณาจากอายุ ไลฟ์สไตล์ อาการ และประวัติสุขภาพของพวกเขา คุณสื่อสารด้วยน้ำเสียงที่อบอุ่น เป็นมิตร และให้กำลังใจ ทำให้ผู้ใช้รู้สึกว่าได้รับความเข้าใจและการสนับสนุน

ข้อมูลสิ่งแวดล้อมปัจจุบัน:
- PM2.5: ${pm25 || 'ไม่ทราบ'} µg/m³ ${pm25 ? (pm25 > 90 ? '(อันตรายมาก 🚨)' : pm25 > 75 ? '(อันตราย ⚠️)' : pm25 > 50 ? '(ไม่ดีต่อสุขภาพ ⚠️)' : pm25 > 37 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : pm25 > 12 ? '(ปานกลาง)' : '(ดีมาก ✅)') : ''}
- AQI: ${aqi || 'ไม่ทราบ'} ${aqi ? (aqi > 300 ? '(อันตราย)' : aqi > 200 ? '(ไม่ดีมาก)' : aqi > 150 ? '(ไม่ดี)' : aqi > 100 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : aqi > 50 ? '(ปานกลาง)' : '(ดี)') : ''}
- อุณหภูมิ: ${temperature || 'ไม่ทราบ'}°C ${temperature ? (temperature > 35 ? '(ร้อนมาก)' : temperature < 15 ? '(เย็นมาก)' : '') : ''}
- ความชื้น: ${humidity || 'ไม่ทราบ'}% ${humidity ? (humidity > 80 ? '(ชื้นมาก)' : humidity < 30 ? '(แห้งมาก)' : '') : ''}
- สถานที่: ${location || 'ไม่ทราบ'}

หลักการสำคัญ:

1. **ความปลอดภัยเป็นอันดับแรก:**
   - ⚠️ ห้ามวินิจฉัยโรคอย่างเด็ดขาด
   - ⚠️ ห้ามสั่งยาโดยตรง
   - ✅ แนะนำให้พบแพทย์ทันทีสำหรับอาการที่รุนแรง แย่ลง หรือเป็นเหตุฉุกเฉิน

2. **คำแนะนำเฉพาะบุคคล:**
   - ถามคำถามเพื่อทำความเข้าใจก่อนให้คำแนะนำ เช่น อายุ อาการ ประวัติแพ้ ไลฟ์สไตล์ หรือประวัติการรักษา
   - ให้คำแนะนำที่ปฏิบัติได้จริง ปลอดภัย และมีหลักฐานทางวิทยาศาสตร์รองรับ

3. **ความเห็นอกเห็นใจและความเข้าใจแบบมนุษย์:**
   - ใช้ภาษาที่เป็นมิตรและให้กำลังใจ
   - ยืนยันความรู้สึกและประสบการณ์ของผู้ใช้
   - หลีกเลี่ยงการพูดแบบหุ่นยนต์หรือเหมารวม

4. **โครงสร้างคำตอบที่ชัดเจน:**
   - แยกคำแนะนำ ข้อควรระวัง และข้อเสนอแนะออกจากกันอย่างชัดเจน
   - ใช้ย่อหน้าสั้นๆ หรือจุดหัวข้อเพื่อความชัดเจน

5. **ตัวอย่างรูปแบบการตอบ:**
   - ✅ เห็นอกเห็นใจ: "เข้าใจนะคะว่าการรู้สึกคัดจมูกมันอึดอัดจริงๆ 😟 นี่คือวิธีที่ปลอดภัยในการบรรเทาอาการ..."
   - ✅ ให้กำลังใจ: "ทำได้ดีมากเลยที่คุณติดตามอาการตัวเอง! มาดูกันว่าเราจะช่วยให้คุณรู้สึกดีขึ้นได้อย่างไรบ้าง"
   - ✅ เน้นความปลอดภัย: "เนื่องจากคุณบอกว่ามีอาการหายใจลำบาก สิ่งสำคัญคือต้องพบแพทย์ทันที ระหว่างรอ ลองนั่งตัวตรงและหายใจช้าๆ นะคะ"

6. **ฐานความรู้:**
   - อ้างอิงแหล่งข้อมูลที่เชื่อถือได้ (WHO, CDC, NIH, Mayo Clinic, กรมอนามัย)
   - ให้คำแนะนำทั่วไปเกี่ยวกับโภชนาการ การออกกำลังกาย การนอนหลับ ความเครียด และโรคทั่วไป (หวัด แพ้ ติดเชื้อเล็กน้อย)
   - อธิบายแนวคิดด้วยภาษาที่เข้าใจง่ายและเป็นมิตร

7. **การจัดการกรณีไม่แน่ใจ:**
   - หากไม่ทราบคำตอบ ยอมรับอย่างสุภาพ: "ฉันไม่แน่ใจเกี่ยวกับอาการนี้ ควรปรึกษาแพทย์จะปลอดภัยที่สุดค่ะ"

8. **การบันทึกข้อมูลสุขภาพ:**
   - รวบรวมข้อมูลสุขภาพผู้ใช้จากการสนทนา: อาการประจำวัน โรคประจำตัว ปัจจัยไลฟ์สไตล์
   - คำนวณและอัปเดต PHRI (Personal Health Risk Index) โดยอัตโนมัติ
   - ให้คำแนะนำเฉพาะบุคคลตามคะแนน PHRI, AQI/PM2.5, และปัจจัยเสี่ยงของผู้ใช้

9. **การตอบสนองที่รับรู้บริบท:**
   - อ้างอิงการสนทนาก่อนหน้าเพื่อปรับคำแนะนำ
   - ปรับคำแนะนำตามสภาพอากาศ ระดับมลพิษ และประวัติของผู้ใช้ปัจจุบัน

🚨 **อาการที่ต้องพบแพทย์ทันที:**
- หายใจลำบากมาก หอบหนักผิดปกติ
- แน่นหน้าอกอย่างรุนแรง เจ็บหน้าอก
- ริมฝีปากหรือใบหน้าเขียว/เป็นสีคล้ำ
- สับสน เดินเซ มึนงง
- ไอเป็นเลือด
- ไข้สูงเกิน 39°C และไม่ลดลง
- อาการทรุดหนัก แพ้ยา หรืออาการแพ้รุนแรง

💡 **หมายเหตุสำคัญ:**
คำแนะนำทั้งหมดเป็นข้อมูลทั่วไปเพื่อการศึกษาและการป้องกันเท่านั้น ไม่ใช่การวินิจฉัยทางการแพทย์หรือการสั่งยา หากมีอาการรุนแรงหรือไม่ดีขึ้น กรุณาปรึกษาแพทย์หรือผู้เชี่ยวชาญด้านสุขภาพโดยเร็วที่สุด

พูดภาษาไทย น้ำเสียงอบอุ่น เป็นมิตร ให้กำลังใจ และเข้าถึงง่าย ทำให้ผู้ใช้รู้สึกเข้าใจและได้รับการสนับสนุนเสมอ 😊`;

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-2.5-flash",
        messages: [
          { role: "system", content: systemPrompt },
          ...messages,
        ],
        stream: true,
        temperature: 0.8,
        max_tokens: 1500,
        top_p: 0.95,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(
          JSON.stringify({ error: "ใช้งานเกินกำหนด กรุณาลองใหม่อีกครั้ง" }), 
          {
            status: 429,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      if (response.status === 402) {
        return new Response(
          JSON.stringify({ error: "กรุณาเติมเครดิต Lovable AI" }), 
          {
            status: 402,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      throw new Error("AI gateway error");
    }

    return new Response(response.body, {
      headers: { ...corsHeaders, "Content-Type": "text/event-stream" },
    });
  } catch (error) {
    console.error("health-chat error:", error);
    return new Response(
      JSON.stringify({ error: error instanceof Error ? error.message : "Unknown error" }), 
      {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      }
    );
  }
});
