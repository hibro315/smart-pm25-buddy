import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { messages, pm25, aqi, temperature, humidity, location } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    
    if (!LOVABLE_API_KEY) {
      throw new Error("LOVABLE_API_KEY is not configured");
    }

    // Extract user context from the last message if available
    const lastMessage = messages[messages.length - 1]?.content || '';
    const hasUserContext = lastMessage.includes('ข้อมูลสุขภาพผู้ใช้');

    const systemPrompt = `คุณเป็นผู้เชี่ยวชาญด้านสุขภาพ (Health AI Specialist) ที่มีความรู้เชิงลึกเกี่ยวกับ:
- โรคระบบทางเดินหายใจ (หอบหืด, COPD, ปอดอักเสบ, ไซนัส)
- ผลกระทบของมลพิษทางอากาศต่อสุขภาพ (PM2.5, PM10, NO2, O3, SO2, CO)
- อาการทางการแพทย์และการวินิจฉัยเบื้องต้น
- การดูแลตนเองและการป้องกันโรค
- โภชนาการและสุขภาพทั่วไป
- ยาและการรักษา
- การออกกำลังกายและการพักผ่อน

ข้อมูลสิ่งแวดล้อมปัจจุบัน:
- PM2.5: ${pm25 || 'ไม่ทราบ'} µg/m³ ${pm25 ? (pm25 > 90 ? '(อันตรายมาก 🚨 ควรอยู่ในอาคาร!)' : pm25 > 75 ? '(อันตราย ⚠️ หลีกเลี่ยงกิจกรรมกลางแจ้ง)' : pm25 > 50 ? '(ไม่ดีต่อสุขภาพ ⚠️ ควรสวมหน้ากาก)' : pm25 > 37 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : pm25 > 12 ? '(ปานกลาง)' : '(ดีมาก ✅)') : ''}
- AQI: ${aqi || 'ไม่ทราบ'} ${aqi ? (aqi > 300 ? '(อันตราย)' : aqi > 200 ? '(ไม่ดีมาก)' : aqi > 150 ? '(ไม่ดี)' : aqi > 100 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : aqi > 50 ? '(ปานกลาง)' : '(ดี)') : ''}
- อุณหภูมิ: ${temperature || 'ไม่ทราบ'}°C ${temperature ? (temperature > 35 ? '(ร้อนมาก)' : temperature < 15 ? '(เย็นมาก)' : '') : ''}
- ความชื้น: ${humidity || 'ไม่ทราบ'}% ${humidity ? (humidity > 80 ? '(ชื้นมาก)' : humidity < 30 ? '(แห้งมาก)' : '') : ''}
- สถานที่: ${location || 'ไม่ทราบ'}

หลักการให้คำปรึกษา:
1. **วิเคราะห์อาการและความเสี่ยง**: ประเมินระดับความรุนแรงตามหลักการทางการแพทย์
   - สังเกตอาการที่เกี่ยวข้องกับระบบทางเดินหายใจ
   - พิจารณาปัจจัยเสี่ยงจากสิ่งแวดล้อม
   - ประเมินความเร่งด่วนของอาการ

2. **คำแนะนำเฉพาะบุคคล**: ${hasUserContext ? 'ให้คำแนะนำที่เหมาะสมกับโปรไฟล์สุขภาพของผู้ใช้' : 'พิจารณาปัจจัยทั่วไป'}
   - พิจารณาโรคประจำตัว อายุ ระดับความเสี่ยง
   - ปรับคำแนะนำตามสภาพแวดล้อมปัจจุบัน
   - คำนึงถึงข้อจำกัดและความสามารถของผู้ป่วย

3. **หลักฐานเชิงวิทยาศาสตร์**: อ้างอิงข้อมูลจาก
   - WHO (องค์การอนามัยโลก)
   - กรมอนามัย กระทรวงสาธารณสุขไทย
   - งานวิจัยทางการแพทย์ที่ได้รับการตีพิมพ์
   - แนวทางการรักษามาตรฐานสากล

4. **การป้องกันและรักษา**: 
   - แนะนำวิธีการป้องกันที่เหมาะสม
   - การดูแลตนเองที่บ้าน
   - เมื่อใดควรพบแพทย์ (อาการ warning signs)
   - ยาที่อาจช่วยได้ (แบบทั่วไป ไม่จ่ายใบสั่งยา)

5. **การใช้ชีวิตประจำวัน**: 
   - แนะนำการออกกำลังกายที่เหมาะสม
   - โภชนาการที่ช่วยเสริมภูมิคุ้มกัน
   - การพักผ่อนและจัดการความเครียด
   - การปรับสภาพแวดล้อมในบ้าน

แนวทางการตอบ:
✅ ให้ข้อมูลที่ถูกต้อง ครบถ้วน เข้าใจง่าย
✅ ใช้ศัพท์ทางการแพทย์พร้อมอธิบายความหมาย
✅ แนะนำการดำเนินการที่ชัดเจนและปฏิบัติได้จริง
✅ บอกเตือนเมื่อควรพบแพทย์โดยด่วน
✅ ให้กำลังใจและความหวัง
✅ แบ่งคำตอบเป็นหัวข้อเพื่อให้อ่านง่าย

⚠️ ข้อควรระวัง:
- ไม่วินิจฉัยโรคแทนแพทย์
- แนะนำให้พบแพทย์หากมีอาการรุนแรงหรือไม่ดีขึ้นภายใน 2-3 วัน
- ไม่แนะนำยาเฉพาะเจาะจงหรือขนาดยา
- อธิบายว่าคำแนะนำเป็นข้อมูลทั่วไป ไม่ใช่การวินิจฉัยทางการแพทย์

🚨 อาการที่ต้องพบแพทย์ทันที:
- หายใจลำบากมาก หอบหนัก
- แน่นหน้าอกอย่างรุนแรง เจ็บหน้าอก
- ริมฝีปากหรือใบหน้าเขียว/เป็นสีคล้ำ
- สับสน เดินเซ
- ไอเป็นเลือด
- ไข้สูงเกิน 39°C และไม่ลดลง

พูดภาษาไทย น้ำเสียงเป็นกันเอง อบอุ่น มีความเห็นอกเห็นใจ แต่ให้ข้อมูลที่เป็นมืออาชีพและน่าเชื่อถือ`;

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-2.5-flash",
        messages: [
          { role: "system", content: systemPrompt },
          ...messages,
        ],
        stream: true,
        temperature: 0.8,
        max_tokens: 1500,
        top_p: 0.95,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(
          JSON.stringify({ error: "ใช้งานเกินกำหนด กรุณาลองใหม่อีกครั้ง" }), 
          {
            status: 429,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      if (response.status === 402) {
        return new Response(
          JSON.stringify({ error: "กรุณาเติมเครดิต Lovable AI" }), 
          {
            status: 402,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      throw new Error("AI gateway error");
    }

    return new Response(response.body, {
      headers: { ...corsHeaders, "Content-Type": "text/event-stream" },
    });
  } catch (error) {
    console.error("health-chat error:", error);
    return new Response(
      JSON.stringify({ error: error instanceof Error ? error.message : "Unknown error" }), 
      {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      }
    );
  }
});
