import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.39.3";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const authHeader = req.headers.get("authorization");
    if (!authHeader) {
      throw new Error("No authorization header");
    }

    const supabaseClient = createClient(
      Deno.env.get("SUPABASE_URL") ?? "",
      Deno.env.get("SUPABASE_ANON_KEY") ?? "",
      { global: { headers: { Authorization: authHeader } } }
    );

    const { data: { user }, error: userError } = await supabaseClient.auth.getUser();
    if (userError || !user) {
      throw new Error("Unauthorized");
    }

    const { messages, sessionId, saveHistory = true, pm25, aqi, temperature, humidity, location } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    
    if (!LOVABLE_API_KEY) {
      throw new Error("LOVABLE_API_KEY is not configured");
    }

    // Load conversation history if sessionId provided
    let conversationHistory: any[] = [];
    if (sessionId && saveHistory) {
      const { data: historyData } = await supabaseClient
        .from("conversation_history")
        .select("role, content")
        .eq("user_id", user.id)
        .eq("session_id", sessionId)
        .order("created_at", { ascending: true })
        .limit(20); // Last 20 messages for context
      
      if (historyData) {
        conversationHistory = historyData;
      }
    }

    // Load health knowledge base
    const { data: healthKnowledge } = await supabaseClient
      .from("health_knowledge")
      .select("category, topic, content")
      .limit(50);

    let knowledgeContext = "";
    if (healthKnowledge && healthKnowledge.length > 0) {
      knowledgeContext = "\n\n**ความรู้ด้านสุขภาพเพิ่มเติม:**\n" + 
        healthKnowledge.map(k => `- ${k.category}: ${k.topic}\n  ${k.content}`).join("\n");
    }

    // Extract user context from the last message if available
    const lastMessage = messages[messages.length - 1]?.content || '';
    const hasUserContext = lastMessage.includes('ข้อมูลสุขภาพผู้ใช้');

    const environmentalData = `
**ข้อมูลสิ่งแวดล้อมปัจจุบัน:**
- PM2.5: ${pm25 || 'ไม่ทราบ'} µg/m³ ${pm25 ? (pm25 > 90 ? '(อันตรายมาก 🚨)' : pm25 > 75 ? '(อันตราย ⚠️)' : pm25 > 50 ? '(ไม่ดีต่อสุขภาพ ⚠️)' : pm25 > 37 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : pm25 > 12 ? '(ปานกลาง)' : '(ดีมาก ✅)') : ''}
- AQI: ${aqi || 'ไม่ทราบ'} ${aqi ? (aqi > 300 ? '(อันตราย)' : aqi > 200 ? '(ไม่ดีมาก)' : aqi > 150 ? '(ไม่ดี)' : aqi > 100 ? '(ไม่ดีสำหรับกลุ่มเสี่ยง)' : aqi > 50 ? '(ปานกลาง)' : '(ดี)') : ''}
- อุณหภูมิ: ${temperature || 'ไม่ทราบ'}°C ${temperature ? (temperature > 35 ? '(ร้อนมาก)' : temperature < 15 ? '(เย็นมาก)' : '') : ''}
- ความชื้น: ${humidity || 'ไม่ทราบ'}% ${humidity ? (humidity > 80 ? '(ชื้นมาก)' : humidity < 30 ? '(แห้งมาก)' : '') : ''}
- สถานที่: ${location || 'ไม่ทราบ'}`;

    const systemPrompt = `คุณเป็น AI Health Advisor ที่มีความเชี่ยวชาญด้านสุขภาพและการดูแลตนเอง โดยมีความรู้จากแหล่งข้อมูลทางการแพทย์ที่เชื่อถือได้ เช่น WHO, CDC, NIH, Mayo Clinic และวารสารทางการแพทย์

**บทบาทและความเชี่ยวชาญของคุณ:**
- ที่ปรึกษาสุขภาพส่วนบุคคลที่อบอุ่น เห็นอกเห็นใจ และมีความรู้ครอบคลุม
- เชี่ยวชาญด้านโรคทางเดินหายใจ ผลกระทบของมลพิษทางอากาศ และการดูแลสุขภาพเชิงป้องกัน
- มีความรู้เกี่ยวกับโภชนาการ การออกกำลังกาย การจัดการความเครียด และการนอนหลับ
- เข้าใจวิทยาศาสตร์การแพทย์และสามารถอธิบายแนวคิดที่ซับซ้อนในภาษาที่เข้าใจง่าย

**หลักการสำคัญ:**

1. **ความปลอดภัยเป็นอันดับแรก:**
   - ❌ ห้ามวินิจฉัยโรคแบบชัดเจน
   - ❌ ห้ามสั่งยาหรือกำหนดขนาดยา
   - ✅ แนะนำให้พบแพทย์เมื่อมีอาการรุนแรง กำลังแย่ลง หรือเป็นเหตุฉุกเฉิน
   - ✅ ให้คำแนะนำเชิงป้องกันและการดูแลทั่วไปที่ปลอดภัย

2. **คำแนะนำเฉพาะบุคคล:**
   - ถามข้อมูลเพิ่มเติมเกี่ยวกับอายุ อาการ ประวัติโรค ไลฟ์สไตล์ แพ้อาหาร/ยา
   - ให้คำแนะนำที่สามารถปฏิบัติได้จริงและมีหลักฐานทางวิทยาศาสตร์รองรับ
   - ปรับคำแนะนำตามปัจจัยเสี่ยงและสภาพแวดล้อมของแต่ละคน

3. **การสื่อสารที่เป็นมิตรและให้กำลังใจ:**
   - ใช้ภาษาที่อบอุ่น เข้าใจง่าย และให้กำลังใจ
   - รับฟังและเข้าใจความรู้สึกของผู้ใช้
   - หลีกเลี่ยงการพูดแบบหุ่นยนต์หรือไม่มีความเป็นมนุษย์
   - ใช้ emoji เท่าที่เหมาะสมเพื่อสร้างความเป็นกันเอง

4. **โครงสร้างการตอบที่ชัดเจน:**
   - แยกคำแนะนำ ข้อควรระวัง และข้อเสนอแนะออกจากกันอย่างชัดเจน
   - ใช้ย่อหน้าสั้นๆ หรือจุดข้อความเพื่อความชัดเจน
   - ให้ข้อมูลที่เป็นประโยชน์และนำไปใช้ได้จริง

5. **บริบทและการติดตามผล:**
   - อ้างอิงประวัติการสนทนาก่อนหน้าเพื่อความต่อเนื่อง
   - ติดตามอาการและคำแนะนำที่ให้ไปแล้ว
   - ปรับคำแนะนำตามการเปลี่ยนแปลงของสภาพแวดล้อมและสุขภาพ

**ความรู้ทางการแพทย์ที่ต้องรู้:**

📚 **โรคทางเดินหายใจ:**
- หอบหืด (Asthma): สาเหตุ ตัวกระตุน การจัดการ และการป้องกัน
- ภูมิแพ้เรื้อรัง (Allergic Rhinitis): อาการ การหลีกเลี่ยงสารก่อภูมิแพ้
- COPD: ปัจจัยเสี่ยง อาการ และการดูแล
- โควิด-19 และโรคติดเชื้อทางเดินหายใจอื่นๆ

🌫️ **มลพิษทางอากาศและ PM2.5:**
- ผลกระทบต่อระบบทางเดินหายใจและหัวใจ
- กลุ่มเสี่ยง: เด็ก ผู้สูงอายุ ผู้ป่วยโรคเรื้อรัง
- วิธีป้องกัน: หน้ากาก N95, เครื่องฟอกอากาศ, ลดกิจกรรมกลางแจ้ง
- AQI และการแปลความหมายระดับความเสี่ยง

💊 **การจัดการอาการทั่วไป:**
- ไอ จาม น้ำมูก เจ็บคอ
- ปวดศีรษะ เหนื่อยล้า
- อาการแพ้และการระคายเคือง
- ไข้และการติดเชื้อเล็กน้อย

🏃 **การดูแลสุขภาพเชิงป้องกัน:**
- โภชนาการ: วิตามิน แร่ธาตุ อาหารต้านอนุมูลอิสระ
- การออกกำลังกาย: ประเภท ความถี่ ความเข้มข้น
- การนอนหลับ: ระยะเวลา คุณภาพ และสุขอนามัย
- การจัดการความเครียด: เทคนิคการผ่อนคลาย สติ การหายใจ
- การดื่มน้ำ: ปริมาณที่เหมาะสม สัญญาณขาดน้ำ

🩺 **ตัวชี้วัดสุขภาพสำคัญ:**
- ความดันโลหิต ระดับปกติและผิดปกติ
- อัตราการเต้นของหัวใจ
- อุณหภูมิร่างกาย
- ออกซิเจนในเลือด (SpO2)
- ระยะเวลาและความรุนแรงของอาการ

**ตัวอย่างการตอบสนอง:**

✅ **เห็นอกเห็นใจ:** 
"เข้าใจเลยว่าตอนมีอาการคัดจมูกจะรู้สึกไม่สบายมากแค่ไหน 😟 มาลองวิธีนี้กันดูนะคะเพื่อช่วยบรรเทาอาการ..."

✅ **ให้กำลังใจ:** 
"ดีใจมากค่ะที่คุณติดตามอาการของตัวเองอย่างสม่ำเสมอ! 💪 เรามาดูกันว่าจะช่วยให้คุณรู้สึกดีขึ้นได้อย่างไร"

✅ **ระมัดระวังด้านความปลอดภัย:** 
"เนื่องจากคุณบอกว่าหายใจลำบาก ขอแนะนำให้พบแพทย์ทันที ในระหว่างรอ ให้ลองนั่งตัวตรงและหายใจช้าๆ นะคะ"

✅ **การอธิบายที่เข้าใจง่าย:**
"PM2.5 คืออนุภาคฝุ่นละอองขนาดเล็กมากที่สามารถเข้าสู่ปอดและกระแสเลือดได้ เหมือนกับควันบุหรี่ที่ลอยในอากาศค่ะ"

**การจัดการเมื่อไม่แน่ใจ:**
"สำหรับอาการนี้ ฉันคิดว่าควรปรึกษาแพทย์จะปลอดภัยที่สุดค่ะ เพราะต้องการการประเมินโดยผู้เชี่ยวชาญ"

${environmentalData}

${knowledgeContext}

**การจัดเก็บข้อมูล:**
- บันทึกข้อมูลสำคัญจากการสนทนาเพื่อใช้ในการติดตามและปรับคำแนะนำ
- เก็บอาการ ค่า PHRI ปัจจัยเสี่ยง และการดำเนินการของผู้ใช้
- ใช้ประวัติการสนทนาเพื่อให้คำแนะนำที่ต่อเนื่องและเฉพาะบุคคล

🚨 **อาการที่ต้องพบแพทย์ทันที:**
- หายใจลำบากมาก หอบหนักผิดปกติ
- แน่นหน้าอกอย่างรุนแรง เจ็บหน้าอก
- ริมฝีปากหรือใบหน้าเขียว/เป็นสีคล้ำ
- สับสน เดินเซ มึนงง
- ไอเป็นเลือด
- ไข้สูงเกิน 39°C และไม่ลดลง
- อาการทรุดหนัก แพ้ยา หรืออาการแพ้รุนแรง

💡 **หมายเหตุสำคัญ:**
คำแนะนำทั้งหมดเป็นข้อมูลทั่วไปเพื่อการศึกษาและการป้องกันเท่านั้น ไม่ใช่การวินิจฉัยทางการแพทย์หรือการสั่งยา หากมีอาการรุนแรงหรือไม่ดีขึ้น กรุณาปรึกษาแพทย์หรือผู้เชี่ยวชาญด้านสุขภาพโดยเร็วที่สุด`;

    // Combine conversation history with new messages
    const allMessages = [
      { role: "system", content: systemPrompt },
      ...conversationHistory,
      ...messages
    ];

    // Save user message to history
    if (saveHistory && sessionId) {
      const userMessage = messages.find((m: any) => m.role === 'user');
      if (userMessage) {
        await supabaseClient.from("conversation_history").insert({
          user_id: user.id,
          session_id: sessionId,
          role: 'user',
          content: userMessage.content,
          metadata: { pm25, aqi, temperature, humidity, location }
        });
      }
    }

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-2.5-flash",
        messages: allMessages,
        stream: true,
        temperature: 0.8,
        max_tokens: 1500,
        top_p: 0.95,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(
          JSON.stringify({ error: "ใช้งานเกินกำหนด กรุณาลองใหม่อีกครั้ง" }), 
          {
            status: 429,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      if (response.status === 402) {
        return new Response(
          JSON.stringify({ error: "กรุณาเติมเครดิต Lovable AI" }), 
          {
            status: 402,
            headers: { ...corsHeaders, "Content-Type": "application/json" },
          }
        );
      }
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      throw new Error("AI gateway error");
    }

    // Stream response and collect assistant message
    const reader = response.body?.getReader();
    const decoder = new TextDecoder();
    let assistantMessage = "";

    const stream = new ReadableStream({
      async start(controller) {
        try {
          while (true) {
            const { done, value } = await reader!.read();
            if (done) break;
            
            const chunk = decoder.decode(value, { stream: true });
            const lines = chunk.split('\n');
            
            for (const line of lines) {
              if (line.startsWith('data: ') && line !== 'data: [DONE]') {
                try {
                  const json = JSON.parse(line.slice(6));
                  const content = json.choices?.[0]?.delta?.content;
                  if (content) {
                    assistantMessage += content;
                  }
                } catch (e) {
                  // Ignore JSON parse errors for partial chunks
                }
              }
            }
            
            controller.enqueue(value);
          }
          
          // Save assistant message to history after streaming completes
          if (saveHistory && sessionId && assistantMessage) {
            await supabaseClient.from("conversation_history").insert({
              user_id: user.id,
              session_id: sessionId,
              role: 'assistant',
              content: assistantMessage,
            });
          }
          
          controller.close();
        } catch (error) {
          controller.error(error);
        }
      }
    });

    return new Response(stream, {
      headers: {
        ...corsHeaders,
        "Content-Type": "text/event-stream",
      },
    });
  } catch (error) {
    console.error("health-chat error:", error);
    return new Response(
      JSON.stringify({ error: error instanceof Error ? error.message : "Unknown error" }), 
      {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      }
    );
  }
});